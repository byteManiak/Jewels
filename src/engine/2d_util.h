#pragma once

/**
 * @brief Check if the given coordinates (x,y) are inside a rectangle
 *        defined by the edges (x1,y1) and (x2,y2).
 */
bool isInBounds(int x, int y, int x1, int y1, int x2, int y2);